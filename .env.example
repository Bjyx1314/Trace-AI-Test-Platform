POSTGRES_USER=testplatform
POSTGRES_PASSWORD=replace-with-a-strong-database-password
POSTGRES_DB=test_platform
JWT_SECRET=replace-with-at-least-32-random-characters
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin
TZ=Asia/Shanghai

# Optional host paths mounted into the backend container.
WEB_FRAMEWORK_PATH=./frameworks/web
API_FRAMEWORK_PATH=./frameworks/api
WORKER_DIST_PATH=./worker-dist
FRAMEWORK_VENV_PATH=./fw-venv
